Faça um programa que determine e mostre os cinco primeiros múltiplos de 3, considerando números
maiores que 0. 

#include<stdio.h>
int main(){
    int numero = 1;
    int contador = 0;
    printf("Apresentando os 5 primeiros múltiplos de 3\n");
    while(contador<5){
        if(numero % 3 == 0){
            printf("%d é múltiplo de 3\n", numero);
            contador = contador + 1;
        }
        numero = numero + 1;
    }
    return 0;
}

Escreva um programa que declare um inteiro, inicialize-o com 0, incremente-o de 100 em 100, imprimindo
seu valor na tela, até que seu valor seja 100000 (cem mil). 

#include<stdio.h>
int main(){
    int numero = 0;
    for(int i=0;i<=100000;i+=100){
        printf("O valor de i é %d\n", i);
    }
    return 0;
}

Faça um programa que leia 10 números e escreva o maior e o menor valor lido

#include<stdio.h>
int main(){
    int numero, maior, menor=0;
    for(int i=0;i<10;i++){
        printf("Digite o valor %d de 10: \n", i+1);
        scanf("%d", &i+1);
        if(numero==0){
            maior = numero;
            menor = numero;
        }
        if(numero>maior){
            maior = numero;
        }
        if(numero<menor){
            numero = menor;
        }
    }
    printf("O número %d é o maior e o número %d é o menor!", maior, menor);
    return 0;
}
    
